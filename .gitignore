node_modules/
dist/
*.tsbuildinfo

# `scripts/grow.mjs` output — model proposals awaiting human review.
# Local working material, not repo content: they are unreviewed by definition,
# and committing them would put un-vetted entries a copy-paste away from the
# dictionary. Curate first, commit the pillar file.
proposals/

# Environment / API keys for scripts/grow.mjs — never commit these.
.env
.env.*

.DS_Store
*.log
npm-debug.log*
.vscode/
.idea/

# Demo bundle — built artifact, regenerated by `npm run build:demo`.
demo/demo.js
site/
